services:
  - type: web
    name: fit-track
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: DATA_DIR
        value: /var/data
    disk:
      name: fit-track-data
      mountPath: /var/data
      sizeGB: 1
